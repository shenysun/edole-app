import { defineComponent } from 'vue';

export default defineComponent({
    name: 'ProjectItem',
    setup() {
        return () => (
            <>
                <div>项目卡片</div>
            </>
        );
    },
});
